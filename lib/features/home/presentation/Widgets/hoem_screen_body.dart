import 'package:flutter/material.dart';

class HoemScreenBody extends StatelessWidget {
  const HoemScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold( 
      body: Column(),
    );
  }
}
